@model List<BookInfoModel>



@if (Model.Count() > 0)
{
    foreach (var bookInfo in Model)
    {
        var book = @bookInfo.VolumeInfo;

        <img src="@book.ImageLinks.Thumbnail" alt="Book Image" />
        <h3>@book.Publisher</h3>

    }
}